Javascript is an interpreted language

Executes without compiling first.
Javascript Runtime reads actua JS code, not bytecode
Exception to this is JIT compilation - performance


JS is dynamically and weakly typed language.

Dynamic Typing 
Run the program and Check types
Static Typing
check types then run the program

We can understand based on how variables are defined
let x=5;  or var
int x=5;
JavaScript supports the so-called agreed-upon requirements of an object-oriented language, that is it can both use objects to model a problem and do so in a modular and reusable way.

 JavaScript currently doesn't have native support for private variables which definitely makes encapsulation harder because you can't hide all the internal implementation details of an object by just using a private keyword as you can in Java.
 
JavaScript is single threaded.
alert - blocks all other operations.
Processing large amounts of data isn't one
of JavaScript's strong suits ‘-
